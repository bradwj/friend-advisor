import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tech.friendadvisor',
  appName: 'FriendAdvisor',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
